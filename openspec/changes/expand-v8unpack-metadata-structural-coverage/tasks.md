## 1. Инвентаризация и доказательные блокеры

- [x] 1.1 Сохранить детерминированный снимок `tn_bp20_custom_mcp` и отчёт,
  воспроизводящий 689/130/559 структурных владельцев и 1 787 событий
  неподдерживаемых граней с разбивкой по семействам, UUID-тегам и ролям.
- [x] 1.2 Классифицировать каждую группу событий по влиянию на существующие
  проекции и отметить недостающие XML↔JSON-доказательства; сумма групп должна
  совпасть с `v8unpack_metadata_unsupported_count`.
- [x] 1.3 Найти XML-корень точно того же CF либо снять блокер вместе с
  пользователем. Для согласованного моста повторный экспорт базы должен
  подтвердить SHA-256 парного CF; парный снимок доказывает только закрытую
  семантику формы, а активный JSON — собственный состав и применимость.

## 2. Оракул и классификация граней

- [x] 2.1 Расширить сравнитель и схему манифеста классами `core`, `projected`,
  `informational`, `blocked` и отдельными core/facet-счётчиками.
- [x] 2.2 Для каждой поддерживаемой грани зафиксировать реальную пару:
  позиционный путь, UUID-тег, XML-семантику, целевую проекцию и нулевую дельту.
- [x] 2.3 Добавить проверки, запрещающие неизвестные коды/роли, автоматическую
  классификацию по UUID или частоте и поддержку грани без парного покрытия.
- [x] 2.4 Зафиксировать автономные манифесты активного контура и проверить их
  детерминизм, хеши, полное покрытие инвентаризации и отсутствие скрытых
  неснятых групп.

## 3. Расширение адаптера и наблюдаемости

- [x] 3.1 Разделить успешность основной структурной проекции владельца и
  покрытие дополнительных граней, сохранив `partial` при blocked-остатке.
- [x] 3.2 Реализовать доказанные `core` и `projected` грани минимальными
  дополнениями закрытых контрактов 802/803 без эвристического обхода JSON.
- [x] 3.3 Добавить публичные facet total/supported/unsupported, согласовать их
  с диагностикой и сохранить прежние identity/core-счётчики.
- [x] 3.4 Подключить расширенный результат к полной и инкрементальной сборке с
  атомарным удалением устаревших строк владельца.
- [x] 3.5 Повысить `BUILDER_VERSION` и проверить штатную перестройку старого
  частичного индекса с прежними флагами.

## 4. Проверки и живая приёмка

- [x] 4.1 Доказать равенство чистой сборки и update по пользовательским
  таблицам, диагностике, core/facet-счётчикам и снимку прочитанных JSON.
- [x] 4.2 На зафиксированном `tn_bp20_custom_mcp` получить
  `structural_total=689`, `structural_indexed=689` и `structural_failed=0`;
  при неснятом core-блокере не отмечать задачу выполненной и запросить решение
  пользователя.
- [x] 4.3 Сверить выборочные строки каждого семейства и каждого нового класса
  грани с прямой XML↔JSON-проекцией и публичными хелперами.
- [x] 4.4 Подтвердить, что оставшиеся informational/blocked-грани видны в
  facet-счётчиках и не позволяют ошибочно объявить весь JSON полным.
- [x] 4.5 Обновить документацию по смыслу core/facet-показателей, ограничениям
  оракула и процедуре добавления следующей грани.
- [x] 4.6 Выполнить профильные тесты кодека/оракула/индекса, полный набор
  проекта, живую перестройку и строгую проверку OpenSpec.
