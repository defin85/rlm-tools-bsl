## 1. Проверочный контракт формата

- [x] 1.1 Завершить `add-v8unpack-bsl-source-support` и повторно использовать
  `SourceFormat.V8UNPACK` и его канонизацию категорий.
- [x] 1.2 Для `v8unpack 1.2.9/obj_version 802` создать парные XML↔JSON эталоны
  всех шести структурных семейств: объект, UUID, русский синоним, каждый
  поддерживаемый вид вложенного элемента, примитивный, ссылочный и составной
  тип.
- [x] 1.3 Зафиксировать в проверочном контракте пути `header`, UUID-теги,
  позиции сгенерированных `TypeId`, отображение типов и ожидаемые строки
  `object_attributes`, `object_synonyms`, `metadata_references`; запретить
  производственному декодеру использовать отсутствующий путь или тег.
- [x] 1.4 Зафиксировать исчерпывающую матрицу идентичности по
  `V8UNPACK_METADATA_IDENTITY_MAP` и проверить её категории/ссылки и
  обязательную пару main/id для каждого присутствующего семейства.
- [x] 1.5 Добавить минимальные реальные фрагменты `header` как положительные
  фикстуры и синтетические фикстуры только для неизвестной формы, неизвестного
  тега, отсутствующего/повреждённого/слишком большого JSON, повторяющегося и
  неразрешённого UUID.

## 2. Схема, адаптер и полная сборка

- [x] 2.1 Повысить `BUILDER_VERSION` с `15` до `16`, добавить внутренние таблицы
  `metadata_objects` и `metadata_type_ids` с неуникальными индексами UUID и
  новые ключи диагностики в `index_meta` и существующие сведения об индексе.
- [x] 2.2 Реализовать общий классификатор белого списка и безопасное чтение
  обычных файлов внутри корня с пределом 16 МиБ.
- [x] 2.3 Реализовать выбор адаптера только для `SourceFormat.V8UNPACK` и строгую
  проверку корня `1.2.9/802`; CF/EDT оставить на прежнем пути.
- [x] 2.4 Реализовать первый проход UUID для обязательной пары `<Kind>.json` /
  `<Kind>.id.json`, извлечение подтверждённых сгенерированных `TypeId` по
  матрице идентичности и второй проход `header` только по структурной шестёрке.
- [x] 2.5 Сформировать существующие строки синонимов, атрибутов и ссылок,
  используя `canonicalize_type_ref` для ссылок и существующее XML-представление
  примитивных типов; для JSON писать путь основного дескриптора и `line=NULL`.
- [x] 2.6 При любом неразрешённом компоненте составного типа оставлять
  `attr_type=NULL` для всей строки и не создавать ссылки этой строки.
- [x] 2.7 Реализовать статусы, точные единицы identity/structural счётчиков,
  семь кодов и закрытые роли диагностики, группировку
  `{code, role, count, examples}`, предел 50 групп и пять примеров.
- [x] 2.8 Реализовать `disabled` для `build_metadata=False` и независимое
  отключение синонимов без ложного `partial`.

## 3. Навигация и инкрементальное обновление

- [x] 3.1 Подключить тот же классификатор к `file_paths`, не добавляя `.json` в
  общий `_FILE_NAV_EXTENSIONS`; проверить наличие только `Configuration.json`,
  основных и `.id.json` файлов матрицы идентичности.
- [x] 3.2 До фильтров git- и негитового путей атомарно заменить весь JSON-слой
  под `SAVEPOINT`, не перестраивая BSL-слой.
- [x] 3.3 Сохранять детерминированный снимок белого списка
  `(path, owner, size, mtime_ns)` для наблюдаемости и восстановления корня.
- [x] 3.4 При каждом обновлении повторять глобальное UUID-разрешение и второй
  проход ссылок по всем владельцам без полной переиндексации BSL.
- [x] 3.5 Гарантировать, что повреждённый обязательный дескриптор удаляет старые
  структурные строки владельца, сохраняет строки независимых объектов и
  переводит статус в `partial`.
- [x] 3.6 При любой дельте `Configuration.json` атомарно обновлять весь
  JSON-слой; при переходе ранее сохранённого `v8unpack` в `UNKNOWN` очистить
  строки по старому снимку, сохранить root-stat/recovery marker, а после
  исправления безусловно восстановить слой до обычного фильтра дельт.

## 4. Доказательство корректности

- [x] 4.1 Сравнить нормализованные множества `object_attributes`,
  `object_synonyms` и `metadata_references` для одной и той же конфигурации в
  CF XML и `v8unpack`; отдельно проверить составные типы, ссылки и реквизиты
  табличных частей.
- [x] 4.2 Проверить `metadata_objects` по парным `.id.json` и UUID XML, а
  `metadata_type_ids` — по `GeneratedType/TypeId` того же XML, включая
  неоднозначный UUID.
- [x] 4.3 Проверить существующие запросы атрибутов, синонимов и входящих ссылок,
  точные значения статуса, счётчиков и диагностик.
- [x] 4.4 Для добавления, изменения, удаления, переименования, смены UUID,
  повреждения и исправления сравнить чистую сборку с git- и негитовым
  обновлением по таблицам, снимку и диагностике.
- [x] 4.5 Проверить, что неизвестная корневая версия даёт `unsupported`,
  неизвестная форма владельца — `partial` с сохранённой идентичностью,
  неразрешённый UUID не создаёт ложную ссылку, а `.elem.json`, формы и макеты
  не читаются и не попадают в `file_paths`.
- [x] 4.6 Проверить дельты корневого `Configuration.json`, режимы
  `build_metadata=False` и `build_synonyms=False`, а также независимую
  навигацию при отключённых метаданных. Отдельно проверить негитовую
  последовательность correct → corrupt → update → fix → update.
- [x] 4.7 Проверить переход индекса версии `15` через принудительную полную
  перестройку с сохранением флагов сборки и откат через перестройку предыдущей
  версией.
- [x] 4.8 Запустить полный набор тестов и линтер, включая регрессию CF/EDT.
- [x] 4.9 На малой и большой реальных выгрузках выполнить три прогона,
  подтвердить предел 120 секунд и 512 МиБ, записать число открытых файлов,
  объектов, предупреждений и размер индекса.
- [x] 4.10 Обновить документацию: обе матрицы `1.2.9/802`, статус полноты,
  отключённые режимы, ограничения, диагностика и обязательная перестройка.
