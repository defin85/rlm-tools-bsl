## 1. Полнотекстовый поиск выбранного корня

- [x] 1.1 Добавить регрессионную проверку `git_search` для корня, целиком
  исключённого родительским `.gitignore`: совпадения в BSL и JSON должны
  находиться, а смешанное состояние файлов не должно создавать дубликаты.
- [x] 1.2 Перевести общий вызов `git grep` на файловый режим `--no-index
  --no-exclude-standard` относительно `base_path`, удалить несовместимый
  `--untracked` и сохранить текущую обработку литералов, POSIX ERE, регистра,
  NUL-разделителей и ошибок процесса.
- [x] 1.3 Проверить сохранение фильтров `path`, `file_types`, `exclude_path`,
  режимов `lines/files`, ограничения выдачи, признака усечения, тайм-аута и
  отклонения небезопасных путей.
- [x] 1.4 Исключить `.git` на любой глубине и доказать, что поиск не следует по
  символьной ссылке на каталог вне выбранного корня, не скрывая остальные
  игнорируемые файлы.
- [x] 1.5 Сохранить текущую автоматическую регистрацию `git_search` только для
  корня внутри рабочего дерева при доступном Git, режимы `never` и `force`, а
  также отсутствие хелпера вне репозитория или при недоступном Git.
- [x] 1.6 Проверить паритет литерального быстрого пути `safe_grep` для точного
  списка отслеживаемых и игнорируемых BSL-файлов и его Python-fallback при
  отказе `git grep`.

## 2. Безопасный fallback parse_form

- [x] 2.1 Добавить проверку источника `v8unpack` с модулем формы и пустой
  таблицей `form_elements`: `parse_form()` должен вернуть `[]` без
  `FileNotFoundError`.
- [x] 2.2 После пустого индексного результата остановить `parse_form()` для
  `v8unpack` до запуска эвристик CF/EDT, сохранив приоритет непустых индексных
  строк.
- [x] 2.3 Отбрасывать диагностические элементы `glob_files`, не являющиеся
  путями, до чтения файлов во всех ветках `parse_form()`.
- [x] 2.4 Выполнить существующие проверки разбора форм CF/EDT и паритета
  индексного и файлового путей без изменения их результатов.

## 3. Документация и приёмка

- [x] 3.1 Обновить документацию `git_search`: выбранный корень
  ищется независимо от `.gitignore`; зафиксировать, что `parse_form()` пока не
  разбирает JSON-структуру форм `v8unpack`; предупредить, что игнорируемые
  секреты и каталоги сборки внутри выбранного корня также входят в поиск, и
  описать сужение через `path`, `file_types`, `exclude_path`. Синхронно
  обновить докстринг `_git_grep`, описание/рецепт реестра хелперов,
  стратегическую подсказку и снимок справки.
- [x] 3.2 Выполнить профильные проверки `test_git_search.py`,
  `test_form_parsers.py` и `test_v8unpack_support.py`, затем полный набор
  проверок проекта.
- [x] 3.3 На зарегистрированном `tn_bp20_custom_mcp` подтвердить совпадения
  `git_search` в игнорируемых BSL/JSON-файлах и пустой безопасный результат
  `parse_form("АвансовыйОтчет")`.
- [x] 3.4 Подтвердить, что `BUILDER_VERSION` и схема SQLite не изменились и
  существующий индекс не требует перестройки.
- [x] 3.5 Зафиксировать живое время `git_search` на `tn_bp20_custom_mcp` и
  подтвердить завершение в настроенный тайм-аут без обещания отдельного
  производительного норматива.
