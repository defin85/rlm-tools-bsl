## 1. Полный инвентарь обработчиков

- [x] 1.1 Расширить `form-inventory`, чтобы он детерминированно перечислял
  каждый кандидат обработчика обычной формы: семейство, владелец, форму,
  локальную версию, позиционный путь, соседний контекст, сырое событие,
  обработчик, область, элемент и наличие процедуры в модуле.
- [x] 1.2 Сверить инвентарь прямым обходом всех 3 740 обычных форм
  `tn_bp20_custom_mcp`; зафиксировать мультимножества по версии, пути,
  событию и области, включая известные группы `0-26` и `0-27`.
- [x] 1.3 Сопоставить сырой инвентарь с текущими строками `form_elements` v19
  и получить точный перечень извлечённых, пропущенных, неоднозначных и
  ошибочно классифицированных привязок.

## 2. Доказательный одношаговый пробник

- [x] 2.1 Расширить один сценарий пробника: автоматически создать изолированную
  файловую базу, минимальную конфигурацию, исходный и изменённый CF,
  распаковать их тем же `v8unpack` и сохранить нормализованную JSON-дельту.
- [x] 2.2 Для каждого уникального класса «локальная версия + позиционный путь
  + область» изменить ровно одну привязку и доказать расположение события,
  обработчика, элемента, типа и пути данных.
- [x] 2.3 Для надёжно автоматизируемых классов события выполнить одного
  представителя после известного действия в 1С и сохранить машинно читаемый
  журнал фактического вызова; для остальных закрепить закрытый дескриптор и
  согласованную семантику всех представителей; все графические процессы
  запускать в `xpra`.
- [x] 2.4 Зафиксировать версии платформы и `v8unpack`, хеши CF, JSON, дельт и
  журналов; обеспечить повторный запуск одной командой без ручной подготовки.
- [x] 2.5 До изменения декодера предъявить пользователю каждый класс без
  однозначной дельты или фактического вызова и совместно выбрать другой
  представитель, действие либо явное сужение change; не оставлять скрытых
  блокеров реализации.

## 3. Закрытый реестр и декодер

- [x] 3.1 Расширить манифест закрытым реестром всех доказанных форменных и
  элементных классов снимка 802; неизвестные версии, пути, события и области
  оставить безопасно неподдерживаемыми.
- [x] 3.2 Обобщить существующий минимальный декодер без эвристик: форменные
  строки получают `scope=form`, элементные — `scope=element`, точные
  `element_name`, `element_type`, `event`, `handler` и `data_path`.
- [x] 3.3 Вычислять `projections.handlers="complete"` либо `"empty"` только
  после полного просмотра применимого класса; неизвестный класс сохраняет
  `unsupported`, повреждение доказанного класса даёт `failed`.
- [x] 3.4 Сохранить команды обычных форм как `unsupported`, не добавляя их
  распознавание в этот change.
- [x] 3.5 Сохранить один `collect_v8unpack_forms` для build/update, атомарную
  замену строк и метаданных и детерминированный порядок; повысить
  `BUILDER_VERSION` и проверить обязательную перестройку старого индекса.

## 4. Публичный интерфейс и проверки

- [x] 4.1 Добавить тесты каждого доказанного форменного и элементного класса,
  соседних позиций, неизвестных событий и версий, повреждённых структур,
  дубликатов и форм без обработчиков.
- [x] 4.2 Проверить `parse_form()` и фильтр `handler` для `scope=form` и
  `scope=element`, сохранив команды, атрибуты и `projections` как контекст.
- [x] 4.3 Доказать отсутствие регрессии управляемых JSON-, CF- и EDT-форм и
  равенство чистой сборки с update по строкам и метаданным, включая откат
  посередине замены.
- [x] 4.4 Пересобрать `tn_bp20_custom_mcp`; подтвердить 4 037 форм,
  `handlers.unsupported=0`, `handlers.failed=0` и точное равенство
  мультимножеств инвентаря, индекса и результатов `parse_form()`.
- [x] 4.5 Повторить живую выборку документов, справочников, общих форм и
  `внЖурналРегистрации`; прямым чтением JSON и BSL подтвердить возвращённые
  форменные и элементные привязки.
- [x] 4.6 Трижды измерить проход форм и полную сборку: медиана не более
  180 секунд, максимум RSS прохода не более 512 МиБ; расследовать рост времени
  прохода более 20% относительно зафиксированного v19.
- [x] 4.7 Обновить манифест, README, INDEXING, HELPERS, инструкции агента и
  журнал изменений; выполнить автономную проверку оракула, профильные и полные
  тесты, независимое ревью и строгую проверку OpenSpec.
