## 1. Парный контракт JSON-форм

- [ ] 1.1 Зафиксировать реальные XML↔JSON-пары одного CF для объектных и общих
  форм, включая обработчики формы/элементов, команды, обычные и основные
  атрибуты, динамический список и BSL-модуль.
- [ ] 1.2 Добавить автономно проверяемые манифесты с SHA-256 CF, входных
  деревьев, версиями, покрытием семейств и мультимножествами `form_elements`.
- [ ] 1.3 Добавить отрицательные проверки неизвестной версии, тега,
  позиционной формы, повреждённого обязательного JSON, символьной ссылки и
  выхода из корня.

## 2. Сборщик и индекс

- [ ] 2.1 Реализовать закрытую матрицу путей объектных и общих форм
  `v8unpack`, проверку идентичности владельца/формы и безопасное чтение
  `<Kind>Form.json`, `.elem.json`, `.id.json`.
- [ ] 2.2 Реализовать форматный JSON-декодер обработчиков, команд и атрибутов,
  возвращающий существующие 14-полевые строки `_FORM_ELEMENTS_INSERT` и одну
  служебную строку `kind="form"` на каждую обнаруженную форму.
- [ ] 2.3 Добавить ограниченную диагностику форм и счётчики
  total/indexed/failed/unsupported без потери подтверждённых строк других форм.
- [ ] 2.4 Подключить один JSON-сборщик к полной и инкрементальной индексации,
  включить три JSON-файла формы в снимок и обеспечить атомарную замену строк.
- [ ] 2.5 На индексном пути научить `_group_form_rows` обрабатывать маркер
  пустой формы и выводить `module_path` точной заменой
  `*Form.elem.json → *Form.obj.bsl`, сохранив контракты CF/EDT и фильтр
  `form_name`/`handler`.
- [ ] 2.6 Повысить `BUILDER_VERSION` и проверить штатную перестройку индекса
  предыдущей версии с сохранением прежних флагов сборки.

## 3. Проверки и приёмка

- [ ] 3.1 Доказать нулевую XML↔JSON-дельту обработчиков, команд и атрибутов на
  всех зафиксированных парах и равенство чистой сборки с update.
- [ ] 3.2 Выполнить регрессионные проверки `parse_form` для CF, EDT и
  безопасного пустого результата неподдержанной JSON-формы.
- [ ] 3.3 Перестроить `tn_bp20_custom_mcp` и подтвердить непустой
  `parse_form("АвансовыйОтчет")`, корректные формы/модули и выборочные
  обработчики, команды и атрибуты прямым чтением источников.
- [ ] 3.4 Измерить время и размер слоя `form_elements`, выполнить профильные
  тесты, полный набор проекта и строгую проверку OpenSpec.
- [ ] 3.5 Обновить `README`, `INDEXING`, `HELPERS`, сведения об индексе и
  стратегические подсказки, явно описав версионные границы и частичность.
